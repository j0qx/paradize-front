import ListCardOffer from './ListCardOffer';

export default {
  component: ListCardOffer,
  title: 'ListCardOffer',
};

const Template = (args) => <ListCardOffer {...args} />;

export const Primary = Template.bind({});
Primary.args = {};
