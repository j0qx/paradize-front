import SearchDataSettings from './SearchDataSettings';

export default {
  component: SearchDataSettings,
  title: 'SearchDataSettings',
};

const Template = (args) => <SearchDataSettings {...args} />;

export const Primary = Template.bind({});
Primary.args = {};
