import CardOffer from './CardOffer';

export default {
  component: CardOffer,
  title: 'CardOffer',
};

const Template = (args) => <CardOffer {...args} />;

export const Primary = Template.bind({});
Primary.args = {};
