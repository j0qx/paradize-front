import ListCardFavoriteSearch from './ListCardFavoriteSearch';

export default {
  component: ListCardFavoriteSearch,
  title: 'ListCardFavoriteSearch',
};

const Template = (args) => <ListCardFavoriteSearch {...args} />;

export const Primary = Template.bind({});
Primary.args = {};
