import SearchZoneSettings from './SearchZoneSettings';

export default {
  component: SearchZoneSettings,
  title: 'SearchZoneSettings',
};

const Template = (args) => <SearchZoneSettings {...args} />;

export const Primary = Template.bind({});
Primary.args = {};
