const initialState = {
  bars: [],
  medic: [],
  shops: [],
  parcs: [],
  police: [],
  schools: [],
};

const searchReducer = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default searchReducer;
