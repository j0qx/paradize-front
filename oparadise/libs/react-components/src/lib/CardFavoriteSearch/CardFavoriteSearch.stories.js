import CardFavoriteSearch from './CardFavoriteSearch';

export default {
  component: CardFavoriteSearch,
  title: 'CardFavoriteSearch',
};

const Template = (args) => <CardFavoriteSearch {...args} />;

export const Primary = Template.bind({});
Primary.args = {};
